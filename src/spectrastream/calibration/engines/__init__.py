"""Calibration engine implementations.

Each engine implements the protocols in :mod:`.base`. Only the open ramanchada2
engine ships today; a closed engine would register here and expose nothing
beyond the corrected axis.
"""
